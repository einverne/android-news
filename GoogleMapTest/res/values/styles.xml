<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

    <style name="custom_window" parent="@android:style/Theme">
        <item name="android:windowTitleSize">40.0dip</item>
        <item name="android:windowTitleBackgroundStyle">@style/custom_window_title_background</item>
    </style>

    <style name="custom_window_title_text">
        <item name="android:textSize">20.0dip</item>
        <item name="android:textColor">#ffffffff</item>
        <item name="android:shadowColor">#ff333333</item>
        <item name="android:shadowDx">1.0</item>
        <item name="android:shadowDy">1.0</item>
        <item name="android:shadowRadius">2.0</item>
    </style>


    <style name="custom_window_title_background">
        <item name="android:background">@drawable/bg_draw_titlebar</item>
    </style>

    <style name="custom_window2" parent="@android:style/Theme">
        <item name="android:windowTitleSize">60.0dip</item>
        <item name="android:windowTitleBackgroundStyle">@style/custom_window_title_background2</item>
    </style>

    <style name="custom_window_title_background2">
        <item name="android:background">@drawable/bg_draw_titlebar</item>
    </style>

    <style name="splash_window" parent="@android:style/Theme">
        <item name="android:windowTitleSize">0.0dip</item>
        <item name="android:background">@drawable/splash</item>
    </style>

    <style name="transparent_dialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>
    
    <!-- the theme applied to the application or activity -->
    <style name="CustomActivityTheme" parent="@android:style/Theme.Holo.Light">
        <item name="android:actionBarStyle">@style/MyActionBar</item>
        <!-- other activity and action bar styles here -->
    </style>

    <!-- style for the action bar backgrounds -->
    <style name="MyActionBar" parent="@android:style/Widget.Holo.ActionBar">
        <item name="android:background">@color/biaoti_background</item>
    </style>

</resources>